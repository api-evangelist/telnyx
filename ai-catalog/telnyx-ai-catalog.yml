generated: '2026-09-13'
method: probed
source: https://telnyx.com/.well-known/ai-catalog.json
description: 'AI Catalog manifest observed at /.well-known/ai-catalog.json on telnyx.com. The AI Catalog
  Standard is the publishing half of Agentic Resource Discovery (ARD): a JSON manifest listing the agentic
  resources a domain offers — MCP servers, agent cards, skills, APIs — each typed by media type and identified
  by a domain-anchored urn:air: URN. Two verdicts are recorded: a structural grade against the data model,
  and the result of the ARD project''s own conformance CLI run against this exact body. Provider-published
  by construction — served from the provider''s own host; nothing here is derived or generated.'
discovery:
  path: /.well-known/ai-catalog.json
  canonical: false
  predecessor_only: true
  attempts:
  - path: /.well-known/ard.json
    http_status: 404
  - path: /.well-known/ai-catalog.json
    http_status: 200
  host: telnyx.com
conformance:
  spec: AI Catalog 1.0 / ARD v0.91
  grade: near-conformant
  spec_version_declared: '1.0'
  hard_failures: []
  soft_gaps:
  - entry-no-representativeQueries
  - host-identifier-not-did-web
  official_tool:
    tool: ARD conformance-test v0.9.1 (ards-project/ard-spec)
    status: PASS
    critical_errors: 0
    errors: []
    warnings:
    - '[Telnyx API Catalog] No ''representativeQueries''. The semantic index is built from this term,
      so the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD
      entry.'
    - '[Telnyx OpenAPI Specification] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    - '[Telnyx MCP Server Card] No ''representativeQueries''. The semantic index is built from this term,
      so the entry will not be found by search — it is a valid catalog entry but not a discoverable ARD
      entry.'
    - '[Telnyx Agent Skills Index] No ''representativeQueries''. The semantic index is built from this
      term, so the entry will not be found by search — it is a valid catalog entry but not a discoverable
      ARD entry.'
    informational:
    - '[Telnyx API Catalog] Media type ''application/linkset+json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
    - '[Telnyx OpenAPI Specification] Media type ''application/json'' is a valid application extension
      media type. ARD permits extension types without core registration.'
    - '[Telnyx MCP Server Card] Media type ''application/json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
    - '[Telnyx Agent Skills Index] Media type ''application/json'' is a valid application extension media
      type. ARD permits extension types without core registration.'
  did_web: null
catalog:
  host_display_name: Telnyx
  host_identifier: telnyx.com
  entry_count: 4
  types:
    application/json: 3
    application/linkset+json: 1
  representative_queries: 0
  trust_manifests: 4
  file: telnyx-ai-catalog.json
transport:
  http_status: 200
  content_type: application/ai-catalog+json
  access_control_allow_origin: '*'
x-evidence:
  fetched: '2026-09-13'
  url: https://telnyx.com/.well-known/ai-catalog.json
  http_status: 200
  content_type: application/ai-catalog+json
  note: Verbatim body saved alongside this manifest. No field inferred.
